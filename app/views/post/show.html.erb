<h1><%=@post.title %></h1>
<p><%=@post.description %></p>

<%= link_to "Edit", post_edit_path(@post) %>
<%= link_to "Delete", post_delete_path(@post), method: :delete, data: {confirm: "Are you sure?"} %>
